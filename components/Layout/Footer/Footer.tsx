import { ReactElement } from 'react';
import styles from './Footer.module.scss';

export default function Header(): ReactElement {
  return (
    <footer>
      <div className={styles.footerContainer}>asdf</div>
    </footer>
  );
}
